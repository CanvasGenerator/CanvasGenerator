# Database Migrations

Use these files for incremental database changes after the first bootstrap.

`database/scalable-schema.sql` remains the full install script for a fresh Supabase project. Files in this folder are smaller patches to apply on an existing database without re-reading the full schema.

