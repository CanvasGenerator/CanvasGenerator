/**
 * Bloc FOOTER 3WA — composant dédié de l'école.
 * Design + couleurs centralisés dans ../school-brand/index.js (moteur commun).
 */
import { registerSchoolFooter } from '../school-brand/index.js';

export default function (editor) {
    registerSchoolFooter(editor, '3wa');
}
